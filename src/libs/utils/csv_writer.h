#ifndef CSV_WRITER_HEADER
#define CSV_WRITER_HEADER

//with_break_line concats break linea at the end of the string
void with_break_line(char *);

//remove_last_character removes last character
void remove_last_character(char *);

#endif
